{
  "name": "clvi-game-client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "verify": "esbuild scripts/verify.mjs --bundle --platform=node --format=esm --log-level=warning --outfile=node_modules/.cache/strata-verify.mjs && node node_modules/.cache/strata-verify.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "devDependencies": {
    "typescript": "~5.6.3",
    "vite": "^5.4.10"
  }
}
